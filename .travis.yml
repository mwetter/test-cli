language: python
python:
  - "2.7"
 
notifications:
  email: false
 
 # Install dependencies
install:
  - echo "#!/bin/bash" > some_command
  - echo "ls -lh \"\$\@\"" >> some_command
  - chmod +x some_command
  - sudo mv some_command /usr/local/bin
  - sudo apt-get install -y python-tidylib
  - pip install git+https://github.com/lbl-srg/BuildingsPy@master
  - shopt -s expand_aliases
  - alias ls="ls -lh"
 

# Execute tests
script:
  - echo ${PATH}
  - pwd
  - cat /usr/local/bin/some_command
  - some_command
  - some_command -m
  - cd Buildings && python ../bin/runUnitTests.py --validate-html-only
 
